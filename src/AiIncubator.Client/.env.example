# Backend API base URL. Leave empty to run the client in mock mode (no backend required).
VITE_API_URL=http://localhost:5153

# Clerk publishable key. Leave empty to disable auth (mock mode) for local UI work.
VITE_CLERK_PUBLISHABLE_KEY=
